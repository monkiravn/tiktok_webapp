# React + Tailwind Frontend

## Run (dev)

```bash
cd frontend
copy .env.example .env  # Windows (or: cp .env.example .env)
npm install
npm run dev  # http://localhost:5173
```

`VITE_API_BASE_URL` should point to your backend (default `http://localhost:8000/api/v1`).
